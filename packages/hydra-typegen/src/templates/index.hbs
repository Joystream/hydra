import { TypeRegistry } from '@polkadot/types'
{{#if hasTypeDefs}}
import { typesJson } from './types-json'
{{/if}}
const typeRegistry = new TypeRegistry()

{{#if hasTypeDefs}}
typeRegistry.register(typesJson)
{{/if}}

export {
  typeRegistry,
}

{{#each modules}}
export * from './{{kebabCase name}}'
{{/each}}
