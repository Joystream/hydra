{
  "name": "@subsquid/hydra-indexer-status-service",
  "private": "true",
  "version": "4.0.0",
  "description": "Indexer status service",
  "license": "MIT",
  "scripts": {
    "build": "tsc",
    "start": "node dist/app.js",
    "docker:build": "docker build . -t hydra-indexer-status-service:latest",
    "docker:tag": "docker tag hydra-indexer-status-service:latest subsquid/hydra-indexer-status-service:${VERSION_TAG} && docker tag hydra-indexer-status-service:latest subsquid/hydra-indexer-status-service:${MAJOR_VERSION_TAG} && docker tag hydra-indexer-status-service:latest subsquid/hydra-indexer-status-service:${RELEASE_TAG:-next}",
    "docker:publish": "export VERSION_TAG=$(node -p 'require(\"./package.json\").version') && export MAJOR_VERSION_TAG=$(echo $VERSION_TAG|cut -d'.' -f1) && yarn docker:build && yarn docker:tag && docker push subsquid/hydra-indexer-status-service --all-tags",
    "lint": "eslint './+(src|test|tools)/**/*.{js,ts}'"
  },
  "dependencies": {
    "express": "^4.17.1",
    "ioredis": "^4.27.6"
  },
  "devDependencies": {
    "@types/express": "^4.17.13",
    "@types/ioredis": "^4.26.6",
    "eslint": "^7.9.0",
    "typescript": "^4.3.5"
  }
}
