{
  "name": "sample-mappings",
  "private": true,
  "version": "5.0.0-alpha.3",
  "description": "Mappings for hydra-processor",
  "main": "lib/mappings/index.js",
  "license": "MIT",
  "scripts": {
    "build": "rimraf lib && yarn tsc --build tsconfig.json && copyfiles generated/**/*.json lib/mappings",
    "lint": "echo \"Skippinng\"",
    "clean": "rimraf lib"
  },
  "dependencies": {
    "@joystream/hydra-common": "^5.0.0-alpha.3",
    "@polkadot/types": "8.9.1"
  },
  "devDependencies": {
    "copyfiles": "^2.4.1",
    "rimraf": "^3.0.2",
    "ts-node": "^10.2.1",
    "typescript": "4.4.2"
  }
}
